# API Services Configuration
# This file contains service definitions for external API integrations

services:
    # Add API service definitions here if needed
    # For example, third-party API clients that need explicit configuration
