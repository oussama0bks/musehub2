security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
<<<<<<< HEAD
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
=======
        users_in_memory: { memory: null }
>>>>>>> 776eda0bf4e6f00382b836d54d37240960c5e114
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
<<<<<<< HEAD
        api:
            pattern: ^/api
            lazy: true
            provider: app_user_provider
            context: main
            # JWT temporarily disabled due to compatibility issue
            # jwt: ~
        main:
            lazy: true
            provider: app_user_provider
            form_login:
                login_path: login
                check_path: login
                default_target_path: home
                success_handler: App\Security\LoginSuccessHandler
            logout:
                path: logout
                target: home
=======
        main:
            lazy: true
            provider: users_in_memory

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true
>>>>>>> 776eda0bf4e6f00382b836d54d37240960c5e114

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
<<<<<<< HEAD
        # Routes publiques - Client peut tout voir
        - { path: ^/login, roles: PUBLIC_ACCESS }
        - { path: ^/logout, roles: PUBLIC_ACCESS }
        - { path: ^/register, roles: PUBLIC_ACCESS }
        - { path: ^/$, roles: PUBLIC_ACCESS }
        - { path: ^/artworks, roles: PUBLIC_ACCESS }
        - { path: ^/events, roles: PUBLIC_ACCESS }
        - { path: ^/marketplace, roles: PUBLIC_ACCESS }
        - { path: ^/community, roles: PUBLIC_ACCESS }
        # API publiques - Client peut consulter, liker, commenter, acheter
        - { path: ^/api/auth/register, roles: PUBLIC_ACCESS }
        - { path: ^/api/auth/login, roles: PUBLIC_ACCESS }
        - { path: ^/api/auth/forgot-password, roles: PUBLIC_ACCESS }
        - { path: ^/api/auth/reset-password, roles: PUBLIC_ACCESS }
        - { path: ^/api/login_check, roles: PUBLIC_ACCESS }
        - { path: ^/api/artworks, roles: PUBLIC_ACCESS }
        - { path: ^/api/events, roles: PUBLIC_ACCESS }
        - { path: ^/api/marketplace, roles: PUBLIC_ACCESS }
        - { path: ^/api/posts, roles: PUBLIC_ACCESS }
        # Routes protégées - Admin uniquement
        - { path: ^/admin, roles: ROLE_ADMIN }
        # Routes protégées - Création/modification nécessite authentification
        - { path: ^/api/artworks, methods: [POST, PUT, DELETE], roles: ROLE_USER }
        - { path: ^/api/posts, methods: [POST, PUT, DELETE], roles: ROLE_USER }
=======
        # - { path: ^/admin, roles: ROLE_ADMIN }
        # - { path: ^/profile, roles: ROLE_USER }
>>>>>>> 776eda0bf4e6f00382b836d54d37240960c5e114

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
